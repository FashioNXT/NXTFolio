<link rel="stylesheet" href="<%= asset_path "search_button.css"%>">

<div class="container-fluid custom-container">
  <div class="row">
    <div class="col-md-10 col-md-offset-1">
      <div id="container-mix"  class="row _post-container_">
        <div class="be-post branddiv" id="Brand1">
          <% @gallery.present? and @gallery.each do |gallery| %>
            <% modinfo=GeneralInfo.find(gallery.GeneralInfo_id) %>
            <% if modinfo.attribute_values[:job_name]=="Brand Owner" %>
                  <div class="col-md-4">
                    <a class="be-img-block">
                      <div class="photo photo-type-line">
                        <% if modinfo.profile_picture? %>
                          <img src="<%= gallery.gallery_picture[0].url %>" alt="omg">
                        <% else %>
                          <img class="profile-img" src="<%= asset_path 'avatar-icon.png' %>">
                        <% end %>
                        <a class="photo-hover" href="<%= galleries_show_path(:project_key => gallery.id) %>">
                          <div class="photo-info">
                            <div class="headline"><%= gallery.gallery_title %></div>
                            <div class="line"></div>
                            <div class="desc"> <%= modinfo.first_name %> <%= modinfo.last_name %> </div>
                            <div class="line"></div>
                            <% if gallery.reviews.blank? %>
                              <% average_review=0 %>
                            <% else %>
                              <% average_review=gallery.reviews.average(:rating).round(2) %>
                            <% end %>
                            <div class="average-review-rating" data-score='<%= average_review %>>'>
                            </div>
                          </div>
                          <div class="mask"></div>
                        </a>
                      </div>
                    </a>
                    <div id="custom-project-title"><a href="<%= galleries_show_path(:project_key => gallery.id) %>"><%= gallery.gallery_title %></a></div>
                    <div id="custom-profile-name"><a href="<%= show_profile_show_profile_path(:user_key => modinfo.userKey) %>"><%= modinfo.first_name %> <%= modinfo.last_name %></a></div>
                  </div>
                <% end %>
              <% end %>
        </div>


        <div class="be-post alldiv" id="all1">
              <% @gallery.present? and @gallery.each do |gallery| %>
                <% modinfo=GeneralInfo.find(gallery.GeneralInfo_id) %>
                  <div class="col-md-4">
                    <a class="be-img-block">
                      <div class="photo photo-type-line">
                        <% if modinfo.profile_picture? %>
                          <img src="<%= gallery.gallery_picture[0].url %>" alt="omg">
                        <% else %>
                          <img class="profile-img" src="<%= asset_path 'avatar-icon.png' %>">
                        <% end %>
                        <a class="photo-hover" href="<%= galleries_show_path(:project_key => gallery.id) %>">
                          <div class="photo-info">
                            <div class="headline"><%= gallery.gallery_title %></div>
                            <div class="line"></div>
                            <div class="desc"> <%= modinfo.first_name %> <%= modinfo.last_name %> </div>
                            <div class="line"></div>
                            <% if gallery.reviews.blank? %>
                              <% average_review=0 %>
                            <% else %>
                              <% average_review=gallery.reviews.average(:rating).round(2) %>
                            <% end %>
                            <div class="average-review-rating" data-score='<%= average_review %>>'>
                            </div>
                          </div>
                          <div class="mask"></div>
                        </a>
                      </div>
                    </a>
                    <div id="custom-project-title"><a href="<%= galleries_show_path(:project_key => gallery.id) %>"><%= gallery.gallery_title %></a></div>
                    <div id="custom-profile-name"><a href="<%= show_profile_show_profile_path(:user_key => modinfo.userKey) %>"><%= modinfo.first_name %> <%= modinfo.last_name %></a></div>
                  </div>
                <% end %>
        </div>



        <div class="be-post othercreatordiv" id="Othercreator1">
          <% @users.present? and @users.each do |modinfo| %>
            <% if modinfo.attribute_values[:job_name]=="Other Creator" %>
              <% @gallery.present? and @gallery.each do |gallery| %>
                <% if gallery.GeneralInfo_id == modinfo.id %>
                  <div class="col-md-4">
                    <a class="be-img-block">
                      <div class="photo photo-type-line">
                        <% if modinfo.profile_picture? %>
                          <img src="<%= gallery.gallery_picture[0].url %>" alt="omg">
                        <% else %>
                          <img class="profile-img" src="<%= asset_path 'avatar-icon.png' %>">
                        <% end %>
                        <a class="photo-hover" href="<%= galleries_show_path(:project_key => gallery.id) %>">
                          <div class="photo-info">
                            <div class="headline"><%= gallery.gallery_title %></div>
                            <div class="line"></div>
                            <div class="desc"> <%= modinfo.first_name %> <%= modinfo.last_name %> </div>
                            <div class="line"></div>
                            <% if gallery.reviews.blank? %>
                              <% average_review=0 %>
                            <% else %>
                              <% average_review=gallery.reviews.average(:rating).round(2) %>
                            <% end %>
                            <div class="average-review-rating" data-score='<%= average_review %>>'>
                            </div>
                          </div>
                          <div class="mask"></div>
                        </a>
                      </div>
                    </a>
                    <div id="custom-project-title"><a href="<%= galleries_show_path(:project_key => gallery.id) %>"><%= gallery.gallery_title %></a></div>
                    <div id="custom-profile-name"><a href="<%= show_profile_show_profile_path(:user_key => modinfo.userKey) %>"><%= modinfo.first_name %> <%= modinfo.last_name %></a></div>
                  </div>
                <% end %>
              <% end %>
            <% end %>
          <% end %>
        </div>


        <div class="be-post influencerdiv" id="Influencer1">
          <% @users.present? and @users.each do |modinfo| %>
            <% if modinfo.attribute_values[:job_name]=="Influencer" %>
              <% @gallery.present? and @gallery.each do |gallery| %>
                <% if gallery.GeneralInfo_id == modinfo.id %>
                  <div class="col-md-4">
                    <a class="be-img-block">
                      <div class="photo photo-type-line">
                        <% if modinfo.profile_picture? %>
                          <img src="<%= gallery.gallery_picture[0].url %>" alt="omg">
                        <% else %>
                          <img class="profile-img" src="<%= asset_path 'avatar-icon.png' %>">
                        <% end %>
                        <a class="photo-hover" href="<%= galleries_show_path(:project_key => gallery.id) %>">
                          <div class="photo-info">
                            <div class="headline"><%= gallery.gallery_title %></div>
                            <div class="line"></div>
                            <div class="desc"> <%= modinfo.first_name %> <%= modinfo.last_name %> </div>
                            <div class="line"></div>
                            <% if gallery.reviews.blank? %>
                              <% average_review=0 %>
                            <% else %>
                              <% average_review=gallery.reviews.average(:rating).round(2) %>
                            <% end %>
                            <div class="average-review-rating" data-score='<%= average_review %>>'>
                            </div>
                          </div>
                          <div class="mask"></div>
                        </a>
                      </div>
                    </a>
                    <div id="custom-project-title"><a href="<%= galleries_show_path(:project_key => gallery.id) %>"><%= gallery.gallery_title %></a></div>
                    <div id="custom-profile-name"><a href="<%= show_profile_show_profile_path(:user_key => modinfo.userKey) %>"><%= modinfo.first_name %> <%= modinfo.last_name %></a></div>
                  </div>
                <% end %>
              <% end %>
            <% end %>
          <% end %>
        </div>


        <div class="be-post modeldiv" id="Model1">
          <% @users.present? and @users.each do |modinfo| %>
            <% if modinfo.attribute_values[:job_name]=="Model" %>
              <% @gallery.present? and @gallery.each do |gallery| %>
                <% if gallery.GeneralInfo_id == modinfo.id %>
                  <div class="col-md-4">
                    <a class="be-img-block">
                      <div class="photo photo-type-line">
                        <% if modinfo.profile_picture? %>
                          <img src="<%= gallery.gallery_picture[0].url %>" alt="omg">
                        <% else %>
                          <img class="profile-img" src="<%= asset_path 'avatar-icon.png' %>">
                        <% end %>
                        <a class="photo-hover" href="<%= galleries_show_path(:project_key => gallery.id) %>">
                          <div class="photo-info">
                            <div class="headline"><%= gallery.gallery_title %></div>
                            <div class="line"></div>
                            <div class="desc"> <%= modinfo.first_name %> <%= modinfo.last_name %> </div>
                            <div class="line"></div>
                            <% if gallery.reviews.blank? %>
                              <% average_review=0 %>
                            <% else %>
                              <% average_review=gallery.reviews.average(:rating).round(2) %>
                            <% end %>
                            <div class="average-review-rating" data-score='<%= average_review %>>'>
                            </div>
                          </div>
                          <div class="mask"></div>
                        </a>
                      </div>
                    </a>
                    <div id="custom-project-title"><a href="<%= galleries_show_path(:project_key => gallery.id) %>"><%= gallery.gallery_title %></a></div>
                    <div id="custom-profile-name"><a href="<%= show_profile_show_profile_path(:user_key => modinfo.userKey) %>"><%= modinfo.first_name %> <%= modinfo.last_name %></a></div>
                  </div>
                <% end %>
              <% end %>
            <% end %>
          <% end %>
        </div>


        <div class="be-post contentdiv" id="Content1">
          <% @users.present? and @users.each do |modinfo| %>
            <% if modinfo.attribute_values[:job_name]=="Content Creator" %>
              <% @gallery.present? and @gallery.each do |gallery| %>
                <% if gallery.GeneralInfo_id == modinfo.id %>
                  <div class="col-md-4">
                    <a class="be-img-block">
                      <div class="photo photo-type-line">
                        <% if modinfo.profile_picture? %>
                          <img src="<%= gallery.gallery_picture[0].url %>" alt="omg">
                        <% else %>
                          <img class="profile-img" src="<%= asset_path 'avatar-icon.png' %>">
                        <% end %>
                        <a class="photo-hover" href="<%= galleries_show_path(:project_key => gallery.id) %>">
                          <div class="photo-info">
                            <div class="headline"><%= gallery.gallery_title %></div>
                            <div class="line"></div>
                            <div class="desc"> <%= modinfo.first_name %> <%= modinfo.last_name %> </div>
                            <div class="line"></div>
                            <% if gallery.reviews.blank? %>
                              <% average_review=0 %>
                            <% else %>
                              <% average_review=gallery.reviews.average(:rating).round(2) %>
                            <% end %>
                            <div class="average-review-rating" data-score='<%= average_review %>>'>
                            </div>
                          </div>
                          <div class="mask"></div>
                        </a>
                      </div>
                    </a>
                    <div id="custom-project-title"><a href="<%= galleries_show_path(:project_key => gallery.id) %>"><%= gallery.gallery_title %></a></div>
                    <div id="custom-profile-name"><a href="<%= show_profile_show_profile_path(:user_key => modinfo.userKey) %>"><%= modinfo.first_name %> <%= modinfo.last_name %></a></div>
                  </div>
                <% end %>
              <% end %>
            <% end %>
          <% end %>
        </div>

        <div class="be-post salesdiv" id="Sales1">
          <% @users.present? and @users.each do |modinfo| %>
            <% if modinfo.attribute_values[:job_name]=="Sales" %>
              <% @gallery.present? and @gallery.each do |gallery| %>
                <% if gallery.GeneralInfo_id == modinfo.id %>
                  <div class="col-md-4">
                    <a class="be-img-block">
                      <div class="photo photo-type-line">
                        <% if modinfo.profile_picture? %>
                          <img src="<%= gallery.gallery_picture[0].url %>" alt="omg">
                        <% else %>
                          <img class="profile-img" src="<%= asset_path 'avatar-icon.png' %>">
                        <% end %>
                        <a class="photo-hover" href="<%= galleries_show_path(:project_key => gallery.id) %>">
                          <div class="photo-info">
                            <div class="headline"><%= gallery.gallery_title %></div>
                            <div class="line"></div>
                            <div class="desc"> <%= modinfo.first_name %> <%= modinfo.last_name %> </div>
                            <div class="line"></div>
                            <% if gallery.reviews.blank? %>
                              <% average_review=0 %>
                            <% else %>
                              <% average_review=gallery.reviews.average(:rating).round(2) %>
                            <% end %>
                            <div class="average-review-rating" data-score='<%= average_review %>>'>
                            </div>
                          </div>
                          <div class="mask"></div>
                        </a>
                      </div>
                    </a>
                    <div id="custom-project-title"><a href="<%= galleries_show_path(:project_key => gallery.id) %>"><%= gallery.gallery_title %></a></div>
                    <div id="custom-profile-name"><a href="<%= show_profile_show_profile_path(:user_key => modinfo.userKey) %>"><%= modinfo.first_name %> <%= modinfo.last_name %></a></div>
                  </div>
                <% end %>
              <% end %>
            <% end %>
          <% end %>
        </div>

        <div class="be-post marketingdiv" id="Marketingdiv1">
          <% @users.present? and @users.each do |modinfo| %>
            <% if modinfo.attribute_values[:job_name]=="Marketing" %>
              <% @gallery.present? and @gallery.each do |gallery| %>
                <% if gallery.GeneralInfo_id == modinfo.id %>
                  <div class="col-md-4">
                    <a class="be-img-block">
                      <div class="photo photo-type-line">
                        <% if modinfo.profile_picture? %>
                          <img src="<%= gallery.gallery_picture[0].url %>" alt="omg">
                        <% else %>
                          <img class="profile-img" src="<%= asset_path 'avatar-icon.png' %>">
                        <% end %>
                        <a class="photo-hover" href="<%= galleries_show_path(:project_key => gallery.id) %>">
                          <div class="photo-info">
                            <div class="headline"><%= gallery.gallery_title %></div>
                            <div class="line"></div>
                            <div class="desc"> <%= modinfo.first_name %> <%= modinfo.last_name %> </div>
                            <div class="line"></div>
                            <% if gallery.reviews.blank? %>
                              <% average_review=0 %>
                            <% else %>
                              <% average_review=gallery.reviews.average(:rating).round(2) %>
                            <% end %>
                            <div class="average-review-rating" data-score='<%= average_review %>>'>
                            </div>
                          </div>
                          <div class="mask"></div>
                        </a>
                      </div>
                    </a>
                    <div id="custom-project-title"><a href="<%= galleries_show_path(:project_key => gallery.id) %>"><%= gallery.gallery_title %></a></div>
                    <div id="custom-profile-name"><a href="<%= show_profile_show_profile_path(:user_key => modinfo.userKey) %>"><%= modinfo.first_name %> <%= modinfo.last_name %></a></div>
                  </div>
                <% end %>
              <% end %>
            <% end %>
          <% end %>
        </div>

        <div class="be-post retaildiv" id="Retail1">
          <% @users.present? and @users.each do |modinfo| %>
            <% if modinfo.attribute_values[:job_name]=="Retail" %>
              <% @gallery.present? and @gallery.each do |gallery| %>
                <% if gallery.GeneralInfo_id == modinfo.id %>
                  <div class="col-md-4">
                    <a class="be-img-block">
                      <div class="photo photo-type-line">
                        <% if modinfo.profile_picture? %>
                          <img src="<%= gallery.gallery_picture[0].url %>" alt="omg">
                        <% else %>
                          <img class="profile-img" src="<%= asset_path 'avatar-icon.png' %>">
                        <% end %>
                        <a class="photo-hover" href="<%= galleries_show_path(:project_key => gallery.id) %>">
                          <div class="photo-info">
                            <div class="headline"><%= gallery.gallery_title %></div>
                            <div class="line"></div>
                            <div class="desc"> <%= modinfo.first_name %> <%= modinfo.last_name %> </div>
                            <div class="line"></div>
                            <% if gallery.reviews.blank? %>
                              <% average_review=0 %>
                            <% else %>
                              <% average_review=gallery.reviews.average(:rating).round(2) %>
                            <% end %>
                            <div class="average-review-rating" data-score='<%= average_review %>>'>
                            </div>
                          </div>
                          <div class="mask"></div>
                        </a>
                      </div>
                    </a>
                    <div id="custom-project-title"><a href="<%= galleries_show_path(:project_key => gallery.id) %>"><%= gallery.gallery_title %></a></div>
                    <div id="custom-profile-name"><a href="<%= show_profile_show_profile_path(:user_key => modinfo.userKey) %>"><%= modinfo.first_name %> <%= modinfo.last_name %></a></div>
                  </div>
                <% end %>
              <% end %>
            <% end %>
          <% end %>
        </div>


        <div class="be-post visualdiv" id="Visual1">
          <% @users.present? and @users.each do |modinfo| %>
            <% if modinfo.attribute_values[:job_name]=="Visual" %>
              <% @gallery.present? and @gallery.each do |gallery| %>
                <% if gallery.GeneralInfo_id == modinfo.id %>
                  <div class="col-md-4">
                    <a class="be-img-block">
                      <div class="photo photo-type-line">
                        <% if modinfo.profile_picture? %>
                          <img src="<%= gallery.gallery_picture[0].url %>" alt="omg">
                        <% else %>
                          <img class="profile-img" src="<%= asset_path 'avatar-icon.png' %>">
                        <% end %>
                        <a class="photo-hover" href="<%= galleries_show_path(:project_key => gallery.id) %>">
                          <div class="photo-info">
                            <div class="headline"><%= gallery.gallery_title %></div>
                            <div class="line"></div>
                            <div class="desc"> <%= modinfo.first_name %> <%= modinfo.last_name %> </div>
                            <div class="line"></div>
                            <% if gallery.reviews.blank? %>
                              <% average_review=0 %>
                            <% else %>
                              <% average_review=gallery.reviews.average(:rating).round(2) %>
                            <% end %>
                            <div class="average-review-rating" data-score='<%= average_review %>>'>
                            </div>
                          </div>
                          <div class="mask"></div>
                        </a>
                      </div>
                    </a>
                    <div id="custom-project-title"><a href="<%= galleries_show_path(:project_key => gallery.id) %>"><%= gallery.gallery_title %></a></div>
                    <div id="custom-profile-name"><a href="<%= show_profile_show_profile_path(:user_key => modinfo.userKey) %>"><%= modinfo.first_name %> <%= modinfo.last_name %></a></div>
                  </div>
                <% end %>
              <% end %>
            <% end %>
          <% end %>
        </div>


        <div class="be-post bloggingdiv" id="Blogging1">
          <% @users.present? and @users.each do |modinfo| %>
            <% if modinfo.attribute_values[:job_name]=="Blogging" %>
              <% @gallery.present? and @gallery.each do |gallery| %>
                <% if gallery.GeneralInfo_id == modinfo.id %>
                  <div class="col-md-4">
                    <a class="be-img-block">
                      <div class="photo photo-type-line">
                        <% if modinfo.profile_picture? %>
                          <img src="<%= gallery.gallery_picture[0].url %>" alt="omg">
                        <% else %>
                          <img class="profile-img" src="<%= asset_path 'avatar-icon.png' %>">
                        <% end %>
                        <a class="photo-hover" href="<%= galleries_show_path(:project_key => gallery.id) %>">
                          <div class="photo-info">
                            <div class="headline"><%= gallery.gallery_title %></div>
                            <div class="line"></div>
                            <div class="desc"> <%= modinfo.first_name %> <%= modinfo.last_name %> </div>
                            <div class="line"></div>
                            <% if gallery.reviews.blank? %>
                              <% average_review=0 %>
                            <% else %>
                              <% average_review=gallery.reviews.average(:rating).round(2) %>
                            <% end %>
                            <div class="average-review-rating" data-score='<%= average_review %>>'>
                            </div>
                          </div>
                          <div class="mask"></div>
                        </a>
                      </div>
                    </a>
                    <div id="custom-project-title"><a href="<%= galleries_show_path(:project_key => gallery.id) %>"><%= gallery.gallery_title %></a></div>
                    <div id="custom-profile-name"><a href="<%= show_profile_show_profile_path(:user_key => modinfo.userKey) %>"><%= modinfo.first_name %> <%= modinfo.last_name %></a></div>
                  </div>
                <% end %>
              <% end %>
            <% end %>
          <% end %>
        </div>


        <div class="be-post financingdiv" id="Financing1">
          <% @users.present? and @users.each do |modinfo| %>
            <% if modinfo.attribute_values[:job_name]=="Financing" %>
              <% @gallery.present? and @gallery.each do |gallery| %>
                <% if gallery.GeneralInfo_id == modinfo.id %>
                  <div class="col-md-4">
                    <a class="be-img-block">
                      <div class="photo photo-type-line">
                        <% if modinfo.profile_picture? %>
                          <img src="<%= gallery.gallery_picture[0].url %>" alt="omg">
                        <% else %>
                          <img class="profile-img" src="<%= asset_path 'avatar-icon.png' %>">
                        <% end %>
                        <a class="photo-hover" href="<%= galleries_show_path(:project_key => gallery.id) %>">
                          <div class="photo-info">
                            <div class="headline"><%= gallery.gallery_title %></div>
                            <div class="line"></div>
                            <div class="desc"> <%= modinfo.first_name %> <%= modinfo.last_name %> </div>
                            <div class="line"></div>
                            <% if gallery.reviews.blank? %>
                              <% average_review=0 %>
                            <% else %>
                              <% average_review=gallery.reviews.average(:rating).round(2) %>
                            <% end %>
                            <div class="average-review-rating" data-score='<%= average_review %>>'>
                            </div>
                          </div>
                          <div class="mask"></div>
                        </a>
                      </div>
                    </a>
                    <div id="custom-project-title"><a href="<%= galleries_show_path(:project_key => gallery.id) %>"><%= gallery.gallery_title %></a></div>
                    <div id="custom-profile-name"><a href="<%= show_profile_show_profile_path(:user_key => modinfo.userKey) %>"><%= modinfo.first_name %> <%= modinfo.last_name %></a></div>
                  </div>
                <% end %>
              <% end %>
            <% end %>
          <% end %>
        </div>

        <div class="be-post otherservicesdiv" id="Otherservices1">
          <% @users.present? and @users.each do |modinfo| %>
            <% if modinfo.attribute_values[:job_name]=="Other Services" %>
              <% @gallery.present? and @gallery.each do |gallery| %>
                <% if gallery.GeneralInfo_id == modinfo.id %>
                  <div class="col-md-4">
                    <a class="be-img-block">
                      <div class="photo photo-type-line">
                        <% if modinfo.profile_picture? %>
                          <img src="<%= gallery.gallery_picture[0].url %>" alt="omg">
                        <% else %>
                          <img class="profile-img" src="<%= asset_path 'avatar-icon.png' %>">
                        <% end %>
                        <a class="photo-hover" href="<%= galleries_show_path(:project_key => gallery.id) %>">
                          <div class="photo-info">
                            <div class="headline"><%= gallery.gallery_title %></div>
                            <div class="line"></div>
                            <div class="desc"> <%= modinfo.first_name %> <%= modinfo.last_name %> </div>
                            <div class="line"></div>
                            <% if gallery.reviews.blank? %>
                              <% average_review=0 %>
                            <% else %>
                              <% average_review=gallery.reviews.average(:rating).round(2) %>
                            <% end %>
                            <div class="average-review-rating" data-score='<%= average_review %>>'>
                            </div>
                          </div>
                          <div class="mask"></div>
                        </a>
                      </div>
                    </a>
                    <div id="custom-project-title"><a href="<%= galleries_show_path(:project_key => gallery.id) %>"><%= gallery.gallery_title %></a></div>
                    <div id="custom-profile-name"><a href="<%= show_profile_show_profile_path(:user_key => modinfo.userKey) %>"><%= modinfo.first_name %> <%= modinfo.last_name %></a></div>
                  </div>
                <% end %>
              <% end %>
            <% end %>
          <% end %>
        </div>

        <div class="be-post manufacturingdiv" id="Manufacturing1">
          <% @users.present? and @users.each do |modinfo| %>
            <% if modinfo.attribute_values[:job_name]=="Manufacturing" %>
              <% @gallery.present? and @gallery.each do |gallery| %>
                <% if gallery.GeneralInfo_id == modinfo.id %>
                  <div class="col-md-4">
                    <a class="be-img-block">
                      <div class="photo photo-type-line">
                        <% if modinfo.profile_picture? %>
                          <img src="<%= gallery.gallery_picture[0].url %>" alt="omg">
                        <% else %>
                          <img class="profile-img" src="<%= asset_path 'avatar-icon.png' %>">
                        <% end %>
                        <a class="photo-hover" href="<%= galleries_show_path(:project_key => gallery.id) %>">
                          <div class="photo-info">
                            <div class="headline"><%= gallery.gallery_title %></div>
                            <div class="line"></div>
                            <div class="desc"> <%= modinfo.first_name %> <%= modinfo.last_name %> </div>
                            <div class="line"></div>
                            <% if gallery.reviews.blank? %>
                              <% average_review=0 %>
                            <% else %>
                              <% average_review=gallery.reviews.average(:rating).round(2) %>
                            <% end %>
                            <div class="average-review-rating" data-score='<%= average_review %>>'>
                            </div>
                          </div>
                          <div class="mask"></div>
                        </a>
                      </div>
                    </a>
                    <div id="custom-project-title"><a href="<%= galleries_show_path(:project_key => gallery.id) %>"><%= gallery.gallery_title %></a></div>
                    <div id="custom-profile-name"><a href="<%= show_profile_show_profile_path(:user_key => modinfo.userKey) %>"><%= modinfo.first_name %> <%= modinfo.last_name %></a></div>
                  </div>
                <% end %>
              <% end %>
            <% end %>
          <% end %>
        </div>



        <div class="be-post materialsdiv" id="Materials1">
          <% @users.present? and @users.each do |modinfo| %>
            <% if modinfo.attribute_values[:job_name]=="Materials" %>
              <% @gallery.present? and @gallery.each do |gallery| %>
                <% if gallery.GeneralInfo_id == modinfo.id %>
                  <div class="col-md-4">
                    <a class="be-img-block">
                      <div class="photo photo-type-line">
                        <% if modinfo.profile_picture? %>
                          <img src="<%= gallery.gallery_picture[0].url %>" alt="omg">
                        <% else %>
                          <img class="profile-img" src="<%= asset_path 'avatar-icon.png' %>">
                        <% end %>
                        <a class="photo-hover" href="<%= galleries_show_path(:project_key => gallery.id) %>">
                          <div class="photo-info">
                            <div class="headline"><%= gallery.gallery_title %></div>
                            <div class="line"></div>
                            <div class="desc"> <%= modinfo.first_name %> <%= modinfo.last_name %> </div>
                            <div class="line"></div>
                            <% if gallery.reviews.blank? %>
                              <% average_review=0 %>
                            <% else %>
                              <% average_review=gallery.reviews.average(:rating).round(2) %>
                            <% end %>
                            <div class="average-review-rating" data-score='<%= average_review %>>'>
                            </div>
                          </div>
                          <div class="mask"></div>
                        </a>
                      </div>
                    </a>
                    <div id="custom-project-title"><a href="<%= galleries_show_path(:project_key => gallery.id) %>"><%= gallery.gallery_title %></a></div>
                    <div id="custom-profile-name"><a href="<%= show_profile_show_profile_path(:user_key => modinfo.userKey) %>"><%= modinfo.first_name %> <%= modinfo.last_name %></a></div>
                  </div>
                <% end %>
              <% end %>
            <% end %>
          <% end %>
        </div>

        <div class="be-post othermakersiv" id="Othermakers1">
          <% @users.present? and @users.each do |modinfo| %>
            <% if modinfo.attribute_values[:job_name]=="Other Makers" %>
              <% @gallery.present? and @gallery.each do |gallery| %>
                <% if gallery.GeneralInfo_id == modinfo.id %>
                  <div class="col-md-4">
                    <a class="be-img-block">
                      <div class="photo photo-type-line">
                        <% if modinfo.profile_picture? %>
                          <img src="<%= gallery.gallery_picture[0].url %>" alt="omg">
                        <% else %>
                          <img class="profile-img" src="<%= asset_path 'avatar-icon.png' %>">
                        <% end %>
                        <a class="photo-hover" href="<%= galleries_show_path(:project_key => gallery.id) %>">
                          <div class="photo-info">
                            <div class="headline"><%= gallery.gallery_title %></div>
                            <div class="line"></div>
                            <div class="desc"> <%= modinfo.first_name %> <%= modinfo.last_name %> </div>
                            <div class="line"></div>
                            <% if gallery.reviews.blank? %>
                              <% average_review=0 %>
                            <% else %>
                              <% average_review=gallery.reviews.average(:rating).round(2) %>
                            <% end %>
                            <div class="average-review-rating" data-score='<%= average_review %>>'>
                            </div>
                          </div>
                          <div class="mask"></div>
                        </a>
                      </div>
                    </a>
                    <div id="custom-project-title"><a href="<%= galleries_show_path(:project_key => gallery.id) %>"><%= gallery.gallery_title %></a></div>
                    <div id="custom-profile-name"><a href="<%= show_profile_show_profile_path(:user_key => modinfo.userKey) %>"><%= modinfo.first_name %> <%= modinfo.last_name %></a></div>
                  </div>
                <% end %>
              <% end %>
            <% end %>
          <% end %>
        </div>


        <div class="be-post photographerdiv" id="Photographer1">
          <% @users.present? and @users.each do |modinfo| %>
            <% if modinfo.attribute_values[:job_name]=="Photographer" %>
              <% @gallery.present? and @gallery.each do |gallery| %>
                <% if gallery.GeneralInfo_id == modinfo.id %>
                  <div class="col-md-4">
                    <a class="be-img-block">
                      <div class="photo photo-type-line">
                        <% if modinfo.profile_picture? %>
                          <img src="<%= gallery.gallery_picture[0].url %>" alt="omg">
                        <% else %>
                          <img class="profile-img" src="<%= asset_path 'avatar-icon.png' %>">
                        <% end %>
                        <a class="photo-hover" href="<%= galleries_show_path(:project_key => gallery.id) %>">
                          <div class="photo-info">
                            <div class="headline"><%= gallery.gallery_title %></div>
                            <div class="line"></div>
                            <div class="desc"> <%= modinfo.first_name %> <%= modinfo.last_name %> </div>
                            <div class="line"></div>
                            <% if gallery.reviews.blank? %>
                              <% average_review=0 %>
                            <% else %>
                              <% average_review=gallery.reviews.average(:rating).round(2) %>
                            <% end %>
                            <div class="average-review-rating" data-score='<%= average_review %>>'>
                            </div>
                          </div>
                          <div class="mask"></div>
                        </a>
                      </div>
                    </a>
                    <div id="custom-project-title"><a href="<%= galleries_show_path(:project_key => gallery.id) %>"><%= gallery.gallery_title %></a></div>
                    <div id="custom-profile-name"><a href="<%= show_profile_show_profile_path(:user_key => modinfo.userKey) %>"><%= modinfo.first_name %> <%= modinfo.last_name %></a></div>
                  </div>
                <% end %>
              <% end %>
            <% end %>
          <% end %>
        </div>


        <div class="be-post agentdiv" id="Agent1">
          <% @users.present? and @users.each do |modinfo| %>
            <% if modinfo.attribute_values[:job_name]=="Agent" %>
              <% @gallery.present? and @gallery.each do |gallery| %>
                <% if gallery.GeneralInfo_id == modinfo.id %>
                  <div class="col-md-4">
                    <a class="be-img-block">
                      <div class="photo photo-type-line">
                        <% if modinfo.profile_picture? %>
                          <img src="<%= gallery.gallery_picture[0].url %>" alt="omg">
                        <% else %>
                          <img class="profile-img" src="<%= asset_path 'avatar-icon.png' %>">
                        <% end %>
                        <a class="photo-hover" href="<%= galleries_show_path(:project_key => gallery.id) %>">
                          <div class="photo-info">
                            <div class="headline"><%= gallery.gallery_title %></div>
                            <div class="line"></div>
                            <div class="desc"> <%= modinfo.first_name %> <%= modinfo.last_name %> </div>
                            <div class="line"></div>
                            <% if gallery.reviews.blank? %>
                              <% average_review=0 %>
                            <% else %>
                              <% average_review=gallery.reviews.average(:rating).round(2) %>
                            <% end %>
                            <div class="average-review-rating" data-score='<%= average_review %>>'>
                            </div>
                          </div>
                          <div class="mask"></div>
                        </a>
                      </div>
                    </a>
                    <div id="custom-project-title"><a href="<%= galleries_show_path(:project_key => gallery.id) %>"><%= gallery.gallery_title %></a></div>
                    <div id="custom-profile-name"><a href="<%= show_profile_show_profile_path(:user_key => modinfo.userKey) %>"><%= modinfo.first_name %> <%= modinfo.last_name %></a></div>
                  </div>
                <% end %>
              <% end %>
            <% end %>
          <% end %>
        </div>



        <div class="be-post designerdiv" id="Designer1">
          <% @users.present? and @users.each do |modinfo| %>
            <% if modinfo.attribute_values[:job_name]=="Designer" %>
              <% @gallery.present? and @gallery.each do |gallery| %>
                <% if gallery.GeneralInfo_id == modinfo.id %>
                  <div class="col-md-4">
                    <a class="be-img-block">
                      <div class="photo photo-type-line">
                        <% if modinfo.profile_picture? %>
                          <img src="<%= gallery.gallery_picture[0].url %>" alt="omg">
                        <% else %>
                          <img class="profile-img" src="<%= asset_path 'avatar-icon.png' %>">
                        <% end %>
                        <a class="photo-hover" href="<%= galleries_show_path(:project_key => gallery.id) %>">
                          <div class="photo-info">
                            <div class="headline"><%= gallery.gallery_title %></div>
                            <div class="line"></div>
                            <div class="desc"> <%= modinfo.first_name %> <%= modinfo.last_name %> </div>
                            <div class="line"></div>
                            <% if gallery.reviews.blank? %>
                              <% average_review=0 %>
                            <% else %>
                              <% average_review=gallery.reviews.average(:rating).round(2) %>
                            <% end %>
                            <div class="average-review-rating" data-score='<%= average_review %>>'>
                            </div>
                          </div>
                          <div class="mask"></div>
                        </a>
                      </div>
                    </a>
                    <div id="custom-project-title"><a href="<%= galleries_show_path(:project_key => gallery.id) %>"><%= gallery.gallery_title %></a></div>
                    <div id="custom-profile-name"><a href="<%= show_profile_show_profile_path(:user_key => modinfo.userKey) %>"><%= modinfo.first_name %> <%= modinfo.last_name %></a></div>
                  </div>
                <% end %>
              <% end %>
            <% end %>
          <% end %>
        </div>




      </div>
    </div>
  </div>
</div>
<script src="<%= asset_path "homepage_logic.js"%>"></script>




